import { Component } from '@angular/core';
import { from } from 'rxjs';


@Component({
  selector: 'app-root',
  templateUrl: './app.component.css',

  

})
export class AppComponent {
  title = 'AutobahnApp';

  displayedColumns = ['position', 'make', 'model', 'year'];
 dataSource = new DataTransfer
}


     export function app() {
       return from([1, 2, 3]);
     }

     export function app2() {
       return from([4, 5, 6]);
     }
